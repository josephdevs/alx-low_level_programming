#include "main.h"

/**
 * main - tests if positive_or_negative() gives correct output when fed 0
 * Return: returns 0 if program successful and !0 otherwise
 */

int main(void)

{

int i;

i = 0;
positive_or_negative(i);

return (0);

}
