#include "main.h"

/**
 * _isupper - Checks if c represents is uppercase.
 * @c: Int representing a character
 *Return: returns 1 if c is uppercase and 0 otherwise.
 */

int _isupper(int c)
{

	if (c >= 65 && c <= 90)

	{
		return (1);
	}

return (0);

}
